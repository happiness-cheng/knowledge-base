# AI Trader — 技术架构文档

## 一句话描述
基于大模型的 A 股自动分析与交易系统，通过 Rule Engine + LLM Agent 双层决策架构，
实现从数据采集、技术分析、AI 风险评估到自动下单的端到端闭环。

## 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    调度层 (main.py)                          │
│  定时触发 → 每5分钟一轮 → 午间总结 → 收盘复盘                  │
└──────────┬──────────────────────────────────────────────────┘
           │
┌──────────▼──────────┐   ┌──────────────────────────────────┐
│  数据采集层          │   │  决策层（双层架构）                │
│                      │   │                                    │
│  新浪财经 API ─┐     │   │  第一层：Rule Engine（规则引擎）    │
│  东方财富 API ─┤     │   │  ├─ MACD 金叉/死叉检测             │
│  curl 子进程   ┘     │   │  ├─ RSI 超买/超卖检测              │
│  (绕过代理TLS问题)    │   │  ├─ 均线交叉检测                   │
│                      │   │  └─ 成交量异常检测                  │
│  输出: JSON/K线数据   │   │  → 筛出"值得关注"的信号             │
└──────────────────────┘   │                                    │
                           │  第二层：LLM Agent（AI 决策）       │
                           │  ├─ 输入: 指标+大盘+板块+历史表现   │
                           │  ├─ 调用: MiMo API (Claude 兼容)   │
                           │  ├─ 输出: 买/卖/观望 + 置信度+理由  │
                           │  └─ 自我修正: 根据历史准确率调整     │
                           │  → 最终决策                         │
                           └──────────┬───────────────────────┘
                                      │
┌─────────────────────────────────────▼──────────────────────┐
│                    执行层                                    │
│                                                              │
│  pywinauto + UIA ─→ 同花顺 v9.50.90 交易窗口                 │
│  ├─ 自动点击"买入/卖出"菜单                                   │
│  ├─ 填写股票代码、数量                                        │
│  └─ 提交委托                                                  │
└──────────┬──────────────────────────────────────────────────┘
           │
┌──────────▼──────────────────────────────────────────────────┐
│                    通知层                                     │
│  飞书 Webhook → 买入/卖出/止损止盈/日报 → 用户手机             │
└──────────────────────────────────────────────────────────────┘
```

## 技术栈

| 模块 | 技术 | 为什么选这个 |
|------|------|-------------|
| 数据采集 | curl + subprocess | Python requests 在 Windows 代理环境下有 TLS 兼容问题，用 curl 子进程绕过 |
| 技术指标 | ta (Python) | 纯 Python 实现，无 C 依赖，支持 MACD/RSI/布林带等 40+ 指标 |
| AI 决策 | MiMo API (Claude 兼容) | 通过 HTTP 直接调 Messages API，支持流式输出 |
| GUI 自动化 | pywinauto + UIA backend | 可操作 Windows 原生应用（同花顺），不需要应用提供 API |
| Web 仪表盘 | FastAPI + uvicorn | 轻量异步框架，Python 实现，前端用原生 HTML/JS |
| 推送通知 | 飞书自定义机器人 Webhook | 免费、无需实名、支持富文本卡片 |
| 图表 | matplotlib (Agg 后端) | 无 GUI 服务器端生成 K 线图 |
| 定时调度 | Windows Task Scheduler | 开机自启动，无需人工干预 |

## 关键设计决策

### 1. 为什么用 Rule Engine + LLM 双层而不是纯 LLM？
- **成本**：全市场 5000 只股票每只都调 API 太贵（~$0.03/次）
- **速度**：规则筛选 < 1 秒/只，LLM 分析 ~10 秒/只
- **方案**：规则引擎快速筛出 5-10 只候选 → LLM 只对候选做深度分析
- **效果**：API 调用减少 99%，决策质量不降

### 2. 为什么用 pywinauto 而不是券商 API？
- 用户还没开户，模拟盘没有官方 API
- pywinauto 通过 UIA 接口操作同花顺 GUI，不依赖应用提供接口
- 缺点：THS 更新可能打破 UI 映射 → 用 automation_id 而非坐标定位

### 3. LLM Agent 的"学习"机制
- 模型权重不变（不做 fine-tuning）
- 通过 Prompt Engineering：每次分析时注入历史准确率统计
- 预测追踪：记录每次 AI 决策 → N 天后自动对比实际结果 → 下次分析时告诉 AI
- 效果：AI 自动调整置信度（准确率低时更保守）

### 4. 日志系统设计
- 每一步独立记录（市场数据/指标/信号/AI决策/交易）
- JSONL 格式，方便程序读取
- 按日期目录组织：`logs/{category}/{YYYY-MM-DD}/{HHMM}.jsonl`
- Claude 可随时读取日志进行复盘分析

## 面试高频考点对应

| 考点 | 本项目体现 |
|------|-----------|
| Agent 设计模式 | 双层决策：Rule(筛选) + LLM(判断) + Tool(下单) |
| Prompt Engineering | 分析提示词设计、历史准确率注入、JSON 结构化输出 |
| RAG 思想 | 每次分析前检索历史预测记录，注入上下文 |
| 微服务架构 | 数据/分析/执行/通知 各模块独立，通过 JSON 文件解耦 |
| 异常处理 | 代理 TLS 问题→curl 绕过；API 限流→频率控制+重试 |
| 自动化测试 | THS UI 探查脚本、模块导入验证、端到端买入测试 |

## 与知识库项目的结合点

```
AI Trader（交易决策）        知识库（知识管理）
     │                            │
     │ 交易记录/分析报告 ──────→   │ 自动归档到知识库
     │                            │
     │ ←──────────────────────    │ 检索历史经验辅助决策
     │                            │
     └──── 共享 Claude API ────────┘
```

- AI Trader 的日志自动同步到知识库
- 知识库提供历史经验给 AI Trader 参考
- 两者共享同一个 LLM 后端
